{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "astro build"
  },
  "deploy": {
    "startCommand": "npx astro dev --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
